import React from 'react'

const UserContext = React.createContext() // its a method, so storing it in a variable

export default UserContext

// Now we have made context, now we have to make a `provider`